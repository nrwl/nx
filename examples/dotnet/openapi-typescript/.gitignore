# Produced by the codegen target; regenerated by `nx run-many -t codegen`.
libs/api-client/src/generated/

# Produced by the .NET build. `openapi/` holds the generated OpenAPI document,
# which nx.json declares as a build output so it rides the build cache.
bin/
obj/
apps/Api/openapi/

# TypeScript build output, and Nx's local cache.
dist/
.nx/
