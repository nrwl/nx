{
  "name": "storefront",
  "targets": {
    "build": {
      "command": "tsc -p apps/storefront/tsconfig.json",
      "dependsOn": ["^build"],
      "inputs": ["{projectRoot}/**/*", { "dependentTasksOutputFiles": "**/*" }],
      "outputs": ["{projectRoot}/dist"],
      "cache": true
    }
  }
}
