
[workspace]
resolver = '2'
members = [
	'packages/nx',
]

[profile.release]
lto = true  # Full optimization for production builds

[profile.dev]
lto = "thin"  # Faster dev builds with good optimization
split-debuginfo = "unpacked"  # Faster linking on macOS
