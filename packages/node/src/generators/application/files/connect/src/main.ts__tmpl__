import connect from 'connect';

const app = connect();

app.use((req, res) => {
     res.end(JSON.stringify({ 'message': 'Hello API' }));
});

app.listen(<%= port %>, () => {
    // Server is running
})