import { <%= className %>BuilderSchema } from './schema';
import builder from './builder';

const options: <%= className %>BuilderSchema = {};

describe('<%= className %> Builder', () => {
  it('can run', async () => {
    const output = await builder(options);
    expect(output.success).toBe(true);
  });
});