/**
 *   const vuePluginImportLine =
    options.uiFramework === 'vue'
      ? `import vue from '@vitejs/plugin-vue';`
      : '';

  const vuePlugin = options.uiFramework === 'vue' ? `vue(),` : '';
 */
