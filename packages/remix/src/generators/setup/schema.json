{
  "$schema": "http://json-schema.org/schema",
  "$id": "NxRemixSetup",
  "title": "",
  "type": "object",
  "properties": {
    "packageManager": {
      "type": "string",
      "description": "The package manager to setup for",
      "enum": ["yarn", "npm", "pnpm"]
    }
  }
}
