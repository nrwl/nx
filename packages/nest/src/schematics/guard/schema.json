{
  "$schema": "http://json-schema.org/schema",
  "id": "NxNestGuard",
  "title": "Create a Nest Guard in a project for Nx",
  "type": "object",
  "examples": [
    {
      "command": "g guard myguard --project=myproject",
      "description": "Generate libs/myproject"
    }
  ],
  "properties": {
    "name": {
      "type": "string",
      "description": "Guard name",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What name would you like to use for the guard?"
    },
    "project": {
      "type": "string",
      "description": "A project where the guard is placed",
      "alias": "p"
    },
    "linter": {
      "description": "The tool to use for running lint checks.",
      "type": "string",
      "enum": ["eslint", "tslint"],
      "default": "tslint"
    },
    "unitTestRunner": {
      "type": "string",
      "enum": ["jest", "none"],
      "description": "Test runner to use for unit tests",
      "default": "jest"
    },
    "tags": {
      "type": "string",
      "description": "Add tags to the guard (used for linting)",
      "alias": "t"
    },
    "skipFormat": {
      "description": "Skip formatting files",
      "type": "boolean",
      "default": false
    },
    "skipTsConfig": {
      "type": "boolean",
      "default": false,
      "description": "Do not update tsconfig.json for development experience."
    }
  },
  "required": ["name"]
}
