{
  "$schema": "http://json-schema.org/schema",
  "id": "NxNestMiddleware",
  "title": "Create a Nest Middleware in a project for Nx",
  "type": "object",
  "examples": [
    {
      "command": "g middleware mymiddleware --project=myproject",
      "description": "Generate libs/myproject"
    }
  ],
  "properties": {
    "name": {
      "type": "string",
      "description": "middleware name",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What name would you like to use for the middleware?"
    },
    "project": {
      "type": "string",
      "description": "A project where the middleware is placed",
      "alias": "p"
    },
    "linter": {
      "description": "The tool to use for running lint checks.",
      "type": "string",
      "enum": ["eslint", "tslint"],
      "default": "tslint"
    },
    "unitTestRunner": {
      "type": "string",
      "enum": ["jest", "none"],
      "description": "Test runner to use for unit tests",
      "default": "jest"
    },
    "tags": {
      "type": "string",
      "description": "Add tags to the middleware (used for linting)",
      "alias": "t"
    },
    "skipFormat": {
      "description": "Skip formatting files",
      "type": "boolean",
      "default": false
    },
    "skipTsConfig": {
      "type": "boolean",
      "default": false,
      "description": "Do not update tsconfig.json for development experience."
    }
  },
  "required": ["name"]
}
