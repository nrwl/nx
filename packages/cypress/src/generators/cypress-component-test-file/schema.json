{
  "$schema": "http://json-schema.org/schema",
  "$id": "NxCypressComponentTestFileProject",
  "cli": "nx",
  "title": "Add Cypress Component test file to project",
  "description": "Add Cypress Component test file for a component.",
  "type": "object",
  "examples": [],
  "properties": {
    "project": {
      "type": "string",
      "description": "The name of the project",
      "$default": {
        "$source": "projectName"
      },
      "x-prompt": "What is the name of the project for this component test file?"
    },
    "name": {
      "type": "string",
      "description": "The name of the component to be tested",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What name would you like to use for the component test file?"
    },
    "componentType": {
      "type": "string",
      "enum": ["react", "next"],
      "description": "Which framework is being used?",
      "alias": "framework",
      "x-prompt": "What framework is being used in this project?"
    },
    "directory": {
      "type": "string",
      "description": "The directory where the component test file should be created from the projects source root",
      "alias": "dir"
    }
  },
  "required": ["name", "project", "componentType"]
}
