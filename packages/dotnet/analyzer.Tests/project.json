{
  "name": "MsbuildAnalyzer.Tests",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "tags": ["dotnet-analyzer"],
  "targets": {
    "build": {
      "executor": "nx:noop",
      "dependsOn": ["build:dotnet"]
    },
    "test": {
      "executor": "nx:noop"
    }
  }
}
