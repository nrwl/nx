{
  "name": "<%= projectName %>",
  "version": "0.0.1",
  "private": true,
  "dependencies": {
    "@react-native/metro-config": "*",<% if (unitTestRunner === 'jest') { %>
    "@testing-library/react-native": "*",<% } %>
    "react": "*",
    "react-native": "*",
    "react-native-svg": "*",<% if (unitTestRunner === 'jest') { %>
    "react-test-renderer": "*",<% } %>
    "metro-config": "*"
  }
}
