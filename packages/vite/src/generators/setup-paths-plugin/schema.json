{
  "cli": "nx",
  "title": "Enables Vite's native tsconfig paths resolution.",
  "description": "Updates vite config files to enable support for workspace libraries via Vite's native `resolve.tsconfigPaths` option.",
  "$id": "setup-paths-plugin-vite-plugin",
  "type": "object",
  "properties": {
    "skipFormat": {
      "description": "Skip formatting files.",
      "type": "boolean",
      "default": false
    }
  }
}
