{
  "name": "<%= name %>",
  "version": "0.0.1",
  "main": "./<%= isUsingTsSolutionConfig ? 'dist/' : '' %>index.js",
  "types": "./<%= isUsingTsSolutionConfig ? 'dist/' : '' %>index.d.ts",
  "exports": {
    ".": {
      "import": "./<%= isUsingTsSolutionConfig ? 'dist/' : '' %>index.mjs",
      "require": "./<%= isUsingTsSolutionConfig ? 'dist/' : '' %>index.js"
    }
  }
}
