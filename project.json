{
  "name": "@nx/nx-source",
  "$schema": "node_modules/nx/schemas/project-schema.json",
  "targets": {
    "local-registry": {
      "executor": "@nx/js:verdaccio",
      "options": {
        "port": 4873,
        "config": ".verdaccio/config.yml",
        "storage": "build/local-registry/storage"
      }
    },
    "populate-local-registry-storage": {
      "cache": true,
      "command": "ts-node -P scripts/tsconfig.scripts.json ./scripts/populate-local-registry-storage.ts",
      "outputs": ["{workspaceRoot}/build/local-registry/storage"]
    }
  }
}
