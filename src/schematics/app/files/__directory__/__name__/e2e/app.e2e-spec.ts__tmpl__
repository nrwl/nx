import { <%= className %>Page } from './app.po';

describe('<%= name %>App', () => {
  let page: <%= className %>Page;

  beforeEach(() => {
    page = new <%= className %>Page();
  });

  it('should display welcome message', () => {
    page.navigateTo();
    expect(page.getParagraphText()).toEqual('Welcome to app!!');
  });
});
