---
title: Code Formatting Reference
description: Learn how Nx detects which code formatter your workspace uses, how oxfmt and Prettier differ, and how to switch between them.
sidebar:
  order: 6
  label: Code formatting
filter: 'type:References'
---

Nx formats code when you run `nx format:write` or `nx format:check`, at the end of most generators,
over the files `nx init` writes, and once over a newly created workspace.
All of them use the same formatter, which Nx detects from your workspace.

Two formatters are supported: [oxfmt](https://oxc.rs) and [Prettier](https://prettier.io).
New workspaces default to oxfmt.

## How Nx picks a formatter

Detection runs in this order, and the first match wins:

1. An oxfmt config file exists at the workspace root.
2. A Prettier config file exists at the workspace root, or `package.json` has a `prettier` key.
3. `package.json` declares an `oxfmt` dependency.

An installed Prettier is not enough on its own, because Prettier is a common transitive dependency.

If nothing matches, `nx format:write` and `nx format:check` warn and exit `0`, and generators skip
formatting.
That is what keeps Nx from reformatting a workspace that uses Biome, dprint, or anything else.

The oxfmt config files Nx looks for:

- `.oxfmtrc.json`
- `.oxfmtrc.jsonc`
- `oxfmt.config.ts`
- `oxfmt.config.mts`

Keep one per directory.
Given two, oxfmt fails with `Both '.oxfmtrc.json' and '.oxfmtrc.jsonc' found` rather than choosing.

### When both are configured

oxfmt wins.
Adding an `.oxfmtrc.json` to a workspace that already has a `.prettierrc` switches the whole
workspace over, including generator formatting.
To go back, delete the oxfmt config.

## Switch an existing workspace to oxfmt

```shell
npm add -D oxfmt
```

```json {% title=".oxfmtrc.json" %}
{
  "singleQuote": true,
  "printWidth": 80
}
```

An Nx-generated `.prettierrc` sets `singleQuote`, and Prettier's own default width is 80, so these
two keep that workspace formatting the way it did before.
If you wrote your own `.prettierrc`, match what it set instead.
oxfmt defaults to double quotes and a width of 100, so anything you leave out moves to those
defaults.

Then delete `.prettierrc` and run `nx format:write` to reformat everything in one commit.

Keep `.prettierignore` if you have one.
oxfmt reads it too, so deleting it starts formatting files you had excluded.

## Generator formatting and `nx format` differ

`nx format:write` runs the formatter's own CLI.
Generators format files that exist only in memory, so Nx applies the configuration itself.
Both read the workspace root config, its `overrides` and `ignorePatterns`, and the root
`.editorconfig`, `.gitignore`, and `.prettierignore`.

Generator formatting reads only the workspace root.
An `.oxfmtrc.json` or `.gitignore` in a subdirectory, or an `.editorconfig` above the root, applies
under `nx format:write` and not during generator formatting.
When a subdirectory config differs from the root one, generated files come out matching the root and
fail `nx format:check` until you run `nx format:write`.

One root-level case leaves the two disagreeing: an `.editorconfig` that sets `indent_size` without
`indent_style`.
Generator formatting applies it and `nx format:write` ignores it, so your generated files fail your
own `nx format:check`.
Set `indent_style` alongside it.

## Skip formatting

Set [`NX_SKIP_FORMAT=true`](/docs/reference/environment-variables) to stop generators and migrations
from formatting.
TypeScript path sorting is separate and stays off by default.

```shell
NX_SKIP_FORMAT=true nx g @nx/react:app my-app
```

## Choose a formatter at workspace creation

`create-nx-workspace` asks which formatter you want when you pick a stack (React, Vue, Angular, or
Node) or the default TypeScript monorepo, and the workspace uses package manager workspaces, which
is the default.
Every other path defaults to oxfmt without asking, including `--no-workspaces` and the `apps`,
`npm`, and `ts-standalone` presets.
Pass `--formatter` on any path to choose explicitly:

```shell
npx create-nx-workspace@latest my-workspace --formatter=prettier
```

The accepted values are `oxfmt`, `prettier`, and `none`.
