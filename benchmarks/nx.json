{
  "$schema": "../node_modules/nx/schemas/nx-schema.json",
  "targetDefaults": {
    "build": {
      "command": "mkdir -p {projectRoot}/dist && cp lorem.md {projectRoot}/dist/output.md",
      "dependsOn": ["^build"],
      "cache": true,
      "inputs": [
        "{projectRoot}/**/*",
        "{workspaceRoot}/lorem.md",
        { "dependentTasksOutputFiles": "**/*" }
      ],
      "outputs": ["{projectRoot}/dist"]
    },
    "cat": {
      "command": "cat lorem.md",
      "cache": true,
      "inputs": ["{projectRoot}/**/*", "{workspaceRoot}/lorem.md"]
    },
    "lint": {
      "command": "mkdir -p {projectRoot}/lint-out && cp lorem.md {projectRoot}/lint-out/output.md",
      "cache": true,
      "inputs": ["{projectRoot}/**/*", "{workspaceRoot}/lorem.md"],
      "outputs": ["{projectRoot}/lint-out"]
    }
  },
  "analytics": false,
  "parallel": 5
}
