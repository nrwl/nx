{
  "name": "benchmarks",
  "version": "0.0.0",
  "private": true,
  "dependencies": {
    "nx": "workspace:*"
  },
  "scripts": {
    "bench": "tsx run-benchmarks.ts",
    "bench:version": "NX_NO_CLOUD=true hyperfine --show-output --setup 'nx reset' --warmup 1 --min-runs 10 --export-json results-version.json 'nx --version'",
    "bench:show-projects": "NX_NO_CLOUD=true hyperfine --show-output --setup 'nx reset' --warmup 1 --min-runs 5 --export-json results-show-projects.json 'nx show projects --tui=false'",
    "bench:cat-warm": "NX_NO_CLOUD=true hyperfine --show-output --setup 'nx reset' --warmup 1 --min-runs 5 --export-json results-cat-warm.json 'nx run-many -t cat --tui=false'",
    "bench:lint-warm": "NX_NO_CLOUD=true hyperfine --show-output --setup 'nx reset' --warmup 1 --min-runs 5 --export-json results-lint-warm.json 'nx run-many -t lint --tui=false'",
    "disabled:bench:build-warm": "NX_NO_CLOUD=true hyperfine --show-output --setup 'nx reset' --warmup 1 --min-runs 5 --export-json results-build-warm.json 'nx run-many -t build --tui=true --tui-auto-exit'"
  },
  "nx": {
    "targets": {
      "bench": {
        "dependsOn": [
          "bench:version",
          "bench:show-projects",
          "bench:cat-warm",
          "bench:lint-warm"
        ]
      },
      "bench:version": {
        "dependsOn": [
          "^build"
        ],
        "parallelism": false
      },
      "bench:show-projects": {
        "dependsOn": [
          "^build"
        ],
        "parallelism": false
      },
      "bench:cat-warm": {
        "dependsOn": ["^build"],
        "parallelism": false
      },
      "bench:lint-warm": {
        "dependsOn": [
          "^build"
        ],
        "parallelism": false
      },
      "disabled:bench:build-warm": {
        "dependsOn": [
          "^build"
        ],
        "parallelism": false
      }
    }
  }
}
