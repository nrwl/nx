---
import { getCollection } from 'astro:content';

const cliDocs = await getCollection('cli-docs');
const cliEntry = cliDocs[0];
---

{cliEntry && cliEntry.rendered && (
  <div set:html={cliEntry.rendered.html} />
)}